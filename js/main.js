'use strict';

const score = 60;
const name = 'taguchi';// && なおかつ
// || もしくは
// ! ~ではない

// if (score >= 50) {
//     if (name === 'taguchi') {
//         console.log('Good Job!');
//     }
// }

if (score >= 50 && name === 'taguchi') {
        console.log('Good Job!');
}